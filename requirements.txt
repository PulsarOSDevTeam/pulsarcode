# pulsarcode runtime dependencies.
# Minimum-version pins for stability; upper bounds keep major-version
# breakage out of the supported install path.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
httpx>=0.28,<1.0
